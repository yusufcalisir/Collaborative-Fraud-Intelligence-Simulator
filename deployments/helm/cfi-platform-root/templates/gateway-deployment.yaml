apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cfi-platform.fullname" . }}-gateway
  labels:
    {{- include "cfi-platform.labels" . | nindent 4 }}
    app: gateway
spec:
  replicas: {{ .Values.gateway.replicaCount }}
  selector:
    matchLabels:
      {{- include "cfi-platform.selectorLabels" . | nindent 6 }}
      app: gateway
  template:
    metadata:
      labels:
        {{- include "cfi-platform.selectorLabels" . | nindent 8 }}
        app: gateway
    spec:
      containers:
        - name: gateway
          image: "{{ .Values.gateway.image.repository }}:{{ .Values.gateway.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.gateway.image.pullPolicy }}
          command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "cfi-platform.fullname" . }}-config
          env:
            - name: SERVICE_NAME
              value: "gateway"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "cfi-platform.fullname" . }}-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "cfi-platform.fullname" . }}-secrets
                  key: JWT_SECRET
            - name: OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "cfi-platform.fullname" . }}-secrets
                  key: OIDC_CLIENT_SECRET
            - name: VAULT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "cfi-platform.fullname" . }}-secrets
                  key: VAULT_TOKEN
          resources:
            {{- toYaml .Values.gateway.resources | nindent 12 }}
